import 'package:flutter/material.dart';

class ServiceCompanyView extends StatelessWidget {
  const ServiceCompanyView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('hello world');
  }
}
